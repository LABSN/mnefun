language: python

python:
  - "3.7"

install:
    # Install mnefun
    - pip install numpy scipy matplotlib patsy pytest pytest-cov pytest-sugar https://api.github.com/repos/mne-tools/mne-python/zipball/master
    - python setup.py install .

script:
    - pytest mnefun
